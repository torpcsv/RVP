# シナリオファイル形式リファレンス

RVPのシナリオファイル(.json)の全要素・親子関係・取りうる値の一覧。
実装(scenario.py)の解析処理に基づく。文字コードはUTF-8(BOM付きも可)。
相対パスはすべて**シナリオファイルのあるフォルダ基準**で解決される。

## 全体構造(親子関係ツリー)

```
シナリオ(トップレベル)
├─ title                     : 文字列
├─ detail                    : 文字列(紹介文、改行可)
├─ background                : 背景イラスト(文字列 or {file,dim})
├─ device_enabled            : true/false(省略=true)
├─ bgm_enabled               : true/false(省略=false)
├─ start                     : イベントID
└─ events                    : { イベントID: イベント, ... }
   └─ イベント(2形式のどちらか)
      ├─ [A] 通常イベント(単一ステート)
      │   ├─ channels        : { "L"|"C"|"R": チャンネル, ... }
      │   ├─ device          : 文字列 or オブジェクト
      │   ├─ bgm             : BGM指定(省略=前のBGMを引き継ぐ)
      │   ├─ end             : 終了条件(省略可)
      │   └─ next            : イベントID or null
      └─ [B] ステート形式イベント
          ├─ states          : { ステートID: ステート, ... }
          │   └─ ステート
          │      ├─ channels : { "L"|"C"|"R": チャンネル, ... }
          │      ├─ device   : 文字列 or オブジェクト
          │      ├─ bgm      : BGM指定(省略=前のBGMを引き継ぐ)
          │      └─ transition : 移行条件(省略可)
          │         ├─ when  : { type, channel, min/max系 }
          │         └─ to    : ステートID or {"random":[...]}
          ├─ start           : 開始ステートID
          ├─ end             : イベント終了条件(必須)
          └─ next            : イベントID or null

チャンネル
├─ mode                      : "sequential" | "random" | "random_bag"
├─ items                     : [ アイテム, ... ]
│   └─ アイテム
│      ├─ audio              : wavパス
│      ├─ tracks             : [ {type, funscript}, ... ]
│      ├─ funscript          : パス or null (旧形式)
│      ├─ weight             : 数値 or {"var":..}
│      └─ pan                : {left, right}
├─ end                       : 終了条件(省略時は既定)
├─ interval                  : 数値 or {min, max}
└─ pan                       : {left, right}
```

## トップレベル

| 要素 | 型 | 必須 | 取りうる値・既定 |
|---|---|---|---|
| `title` | 文字列 | 任意 | 省略時はファイル名。**画面の表示名には使わない**(表示は常に「拡張子を除いたファイル名」)。編集画面は保存時にファイル名から自動でセットする |
| `detail` | 文字列 | 任意 | シナリオの紹介文(作成者の自由記入)。改行(`\n`)可。シナリオタブに表示され、視聴前の事前情報になる。省略時は空。編集画面ではツールバーの「紹介文」ダイアログで編集する |
| `background` | 文字列/オブジェクト | 任意 | **背景イラスト**。再生タブの背景全面に表示するイラストを指定する。`"background": "images/bg.png"`(文字列=暗さ既定40%)、または `{"file": "images/bg.png", "dim": 40}`。`dim` は画像の暗さ%(0〜100の数値、省略=40)。100=真っ黒。**40未満では画像の重なりの濃さが段階的に強化され、0で最大**になる(視聴側の「透け具合」を基準に上乗せ。40以上は従来どおり黒の減光のみ)。パスはシナリオファイル基準の相対(絶対も可)。対応形式は png/jpg/webp/bmp/gif 等(Pillowが読める形式)。再生タブ表示中は**タイトルバーを除く画面全体にイラストが薄く重なり、全コンポーネント越しに見える**(クリック透過オーバーレイ方式。下のUIはそのまま操作できる)。画像はウィンドウの**縦方向にフィット**し、横は中央寄せで左右が見切れる(横へ広げると隠れていた部分が現れ、画像より広い分は黒)。表示のON/OFFと**透け具合(弱/中/強)**は**視聴側の設定**(メイン画面の設定ダイアログ「背景イラスト」)が最終決定する。ファイルが存在しない場合は読み込みエラー |
| `device_enabled` | 真偽値 | 任意 | **デバイス連動のON/OFF**。省略=`true`(従来どおり)。`false` のシナリオは、`tracks` や `device`(担当)の定義がJSONに残っていても**再生時にデバイスを駆動しない**。編集画面はデバイス関連UIを隠し、再生タブは④デバイス調整・⑤グラフを外した3画面になる。紐づけ定義は保持されるので、`true` へ戻せば復活する。編集画面の新規シナリオは**OFFが既定**(保存時に `false` を明示。ONで保存するとキーは書かれない)。true/false 以外は読み込みエラー |
| `bgm_enabled` | 真偽値 | 任意 | **BGM機能のON/OFF**。**省略=`false`**(`device_enabled` とは省略時の意味が逆。BGMを持つ旧シナリオは存在しないため)。`false` のシナリオは、各ノードの `bgm` 定義がJSONに残っていても**再生時にBGMを鳴らさない**(編集画面もBGMブロックを隠す)。定義は保持されるので `true` へ戻せば復活する。保存時は ON=`true` を明示 / OFF=キーを書かない。true/false 以外は読み込みエラー |
| `start` | 文字列 | 必須 | `events` に存在するイベントID |
| `events` | オブジェクト | 必須(※) | キー=イベントID(自由な文字列)、値=イベント |
| `vars` | オブジェクト | 任意 | シナリオ変数の宣言(下記「変数」参照)。変数を使う場合は**宣言必須** |
| `watch` | 配列 | 任意 | 監視(watch)トリガー(下記「変数」章の「監視」参照)。変数条件の成立で指定イベントへ遷移する |
| `map_mode` | 文字列 | 任意 | **イベント遷移図の配置モード**(=299・表示専用)。`"auto"`(省略時=自動配置)/`"manual"`(各イベントの `pos` で配置)。それ以外は読み込みエラー。編集画面は手動=`"manual"` を明示 / 自動=キーを書かない |

※ 旧v1形式では `events` の代わりに `nodes` (後述の後方互換を参照)。

## イベント

`states` キーがあればステート形式、`channels` キーがあれば通常イベント。
どちらも無く `items` キーがあれば旧「items直書き」形式として解釈される
(後方互換を参照)。`states` / `channels` / `items` のいずれも無い場合は
**音声なしイベント**(下記)。

### color(ノードの着色)

イベント/ステートに `"color": "#RRGGBB"` を指定すると、イベント図/
ステート図のノード(丸)がその色で塗られる(例: 危険マス=赤、安全マス=緑)。
**表示専用**で再生の挙動には影響しない。省略時は既定色(通常イベント=灰、
ステート形式イベント=茶系)。形式が `#RRGGBB`(16進6桁)以外は読み込み
エラー。文字色は塗りの明るさから自動で黒系/白系が選ばれる。
編集画面では図のノードを**右クリック**するとカラーパレット(色相7段×
PCCSトーン5列+無彩色+リセット)から選べる。

### pos(イベント遷移図の手動配置)

イベントに `"pos": [x, y]`(数値2つ・図のキャンバス座標=ノード中心)を
指定すると、トップレベル `"map_mode": "manual"` のとき、その位置に
ノードが描かれる(=299)。**表示専用**で再生の挙動には影響しない。
`map_mode` が auto(省略)のときは無視される(値は保持される)。手動モードで
`pos` の無いイベントは自動配置の位置を格子(26px)へ吸着し、他のノードと
重なるなら下へずらした位置に置かれ、編集画面ではその位置が `pos` に
書き戻される。編集画面ではノードをドラッグして動かす(格子へ吸着・
他のノードと重なる/接する位置には置けない)。形式が数値2つの配列以外は
読み込みエラー。

### [A] 通常イベント(単一ステート)

| 要素 | 型 | 必須 | 取りうる値・既定 |
|---|---|---|---|
| `channels` | オブジェクト | 任意 | キーは `"L"` `"C"` `"R"` のみ。**省略/空 `{}` = 音声なしイベント**(下記) |
| `device` | 文字列/オブジェクト | 任意 | 下記「device」参照。省略時は全種別→C(C不在なら先頭ch) |
| `seek_channel` | 文字列 | 任意 | シークバー追従チャンネル(`"L"`/`"C"`/`"R"`、定義済みchのみ)。省略時は自動=C→L→Rの優先順で音声のあるch |
| `end` | オブジェクト | 任意 | 下記「イベントendの解釈」参照 |
| `next` | 文字列/null | 任意 | 次のイベントID。null/省略=シナリオ終了 |

**イベントendの解釈(通常イベント)**:
- 省略時: **linear担当チャンネルの終了条件**に従う(担当ch不在なら先頭ch)
- `{"type": "channel", "channel": "L"}`: 指定チャンネルの終了でイベント終了。
  このときのみ、他チャンネルは `end: {"type": "none"}`(無限)が指定可能。
  終了成立後、他チャンネルは再生中の音声を終えてから停止(ワインドダウン)
- `{"type": "cond", "when": [判定式, ...]}`: **変数の判定式(AND)が成立したら
  イベント終了**。`when` の書式は watch / next-cond と同じ(`{"var","op","value"}`、
  `op` は `== != >= <= > <`、`value` は定数または `{"var": 名}`)。再生中に
  約50ms間隔で常時評価し、全条件成立で再生中の音声を終えてから次イベントへ
  (ワインドダウン)。変数はイベント内の `on_play`/`on_complete`/watch/choice/input
  等で変化させる。この指定時は全チャンネルに `end: {"type": "none"}`(無限)を
  許可する(条件成立まで再生を続けるため)。変数宣言(vars)がある時のみ選択可。
- `{"type": "duration", "seconds": N}`: **イベント開始からの累積経過時間が N 秒を
  過ぎたらイベント終了**(再生中の音声は最後まで=ワインドダウン)。`seconds` は
  定数または `{"var": 名}`。この指定時は全チャンネルに `end: {"type": "none"}`
  (無限)を許可する(指定秒の経過まで再生を続けるため)。編集画面の
  「イベント終了条件」で「合計時間が経過した時」から指定できる。
- `{"type": "duration", "min_seconds": A, "max_seconds": B}`: **duration の範囲抽選**。
  イベントに入るたびに [A, B] の一様乱数 R 秒を抽選し、累積経過が R 秒を超えたら
  終了(ステート移行の `state_time` と同じ範囲抽選)。A/B は 0以上の定数または
  `{"var": 名}`。max 省略=min と同じ(単一)。編集画面では「min秒〜max秒」の2欄で
  指定し、min==max のときは単一 `seconds` として保存される。◀◀ で入り直すと
  抽選し直す(閾値なので毎入場で再抽選)。**同じ書式がステート形式イベントの
  end(合計N秒で次へ)とチャンネルの end(N秒で終了)でも使える**。
- `{"type": "none"}`: **無限**。イベント自身は終わらない。チャンネルが
  全部終わっても**無音のまま待ち続ける**(有限チャンネルとの併用も可。音声を
  1本鳴らしたあと選択肢を出して待つ、という作りができる)。全チャンネルに
  `end: {"type": "none"}`(無限)を許可する。
  **出口は `next` が「なし(null)」「選択肢」「数値入力」「判定式」のいずれかに
  限られる**(下記)。編集画面の「イベント終了条件」で「無限」。
- once/repeat/plays をイベントレベルに直接書くことも可能(sequential扱いで
  解析される。編集画面でUI選択できるのは duration まで=plays は直書きのみ)

**無限(`end: {"type":"none"}`)のときの `next` 制限**:

| next | 可否 | 理由 |
|---|---|---|
| `null`(なし) | ○ | 遷移しない。手動操作(▶▶)か監視(watch)でだけ抜ける |
| `{"choice": [...]}` | ○ | `show` は `"start"` か `{"seconds"}`(`"end"` は永久に来ない) |
| `{"input": {...}}` | ○ | 同上 |
| `{"cond": [...]}` | ○ | **再生中に評価し続け、成立した時点でイベントを打ち切って分岐する**(=「判定式(常に監視)」)。`else` は成立しえないので使わない |
| `"eventB"`(固定) | **×** | 終了時にしか評価されないため永久に発火しない=読み込みエラー |
| `{"random": [...]}`(分岐) | **×** | 同上=読み込みエラー |

> **判定式(常に監視)**: 保存形式は通常の変数分岐と同じ `cond` で、
> **終了条件が「無限」かどうか**で再生側の扱いが変わる。通常(有限)の
> イベントでは従来どおりイベント終了時に1回だけ評価される。

### [A'] 音声なしイベント(分岐専用ノード)

`channels` を**省略または空 `{}`** にすると、音声を再生せず分岐(ランダム/
選択肢/変数分岐/数値入力)や変数操作だけを実行する「音声なしイベント」になる。
編集画面では全チャンネルを無効にすると音声なしとして保存される(channels
キー省略)。

```json
"roll_dice": {
  "on_start": [{"roll": "d", "min": 1, "max": 6}],
  "next": {"cond": [{"when": [{"var": "d", "op": ">=", "value": 4}],
                     "to": "big"}], "else": "small"}
}
```

**意味論(即時通過)**: 入場 → イベントの `on_start` 発火 → watch 評価 →
即座に自然終了扱い → `on_end` 発火 → `next` の評価へ。`run_log` /
イベント状態ビューの軌跡には通常どおり記録される。`next` が選択肢
(`choice`)/数値入力(`input`)の場合は、無音のまま表示されて決定を待つ
(`show` の指定によらず実質「即時表示」)。

**無音待機ノード**: `end` に **`duration`(経過時間)** を指定すると、
その秒数だけ何も再生せずに待ってから `next` へ進む「無音待機ノード」になる。
秒数は単一 `seconds` でも範囲抽選 `min_seconds`〜`max_seconds` でもよく、
どちらも `{"var": 名前}` の変数参照が使える(通常イベントの duration と同じ規則)。
待っている間は一時停止でクロックが止まり、停止操作では即座に抜ける。

```json
"pause_square": {
  "end": {"type": "duration", "min_seconds": 3, "max_seconds": 8},
  "next": "next_square"
}
```

**制約(読み込みエラーになる指定)**:
- `end` は **`duration` のみ**(省略=即時通過)。`cond` / `plays` / `channel`
  などは指定不可。**待っている間に条件を成立させる実行主体(チャンネル)が
  居らず、永久待機になるため**
- `device` は指定不可(担当デバイスが無意味なため)
- `seek_channel` は指定不可(追従する音声が無いため)
- 「チャンネルがあるのに items が空」「アイテムに audio が無い」は従来どおり
  エラー(作りかけの検出は維持)

**安全機構(無限ループ検出)**: 音声なしノード同士だけで巡回すると音声再生
ゼロのまま回り続けるため、音声の再生・選択肢/数値入力の待機を挟まずに
**100回連続で通過すると status="error"+「音声なしイベントの無限ループの
可能性があります」で停止**する。ただし**1秒以上待った通過はカウンタを
リセット**する(待機ノードを多数たどる正当なシナリオを誤検出しない)。
また1通過ごとに約20msの小休止が入り、停止操作・表示追従などUIの応答性が
保たれる。

### [B] ステート形式イベント

| 要素 | 型 | 必須 | 取りうる値・既定 |
|---|---|---|---|
| `states` | オブジェクト | 必須 | キー=ステートID(自由な文字列)、1つ以上 |
| `start` | 文字列 | 必須 | `states` に存在するステートID |
| `end` | オブジェクト | **必須** | `duration` / `plays` / `transitions` / `cond` / `states` / `none`(下表) |
| `next` | 文字列/null | 任意 | 次のイベントID |

**イベント終了条件(ステート形式、ステートを跨いで累積)**:

| type | 追加フィールド | 意味 |
|---|---|---|
| `"duration"` | `seconds`(数値)、>0。または `min_seconds`+`max_seconds`(範囲抽選) | イベント滞在時間の合計。範囲指定はイベントに入るたびに [min, max] の一様乱数を抽選(書式・検証は通常イベントの範囲抽選と同一。各値は定数 or `{"var":名}`。編集画面は「合計N秒で次へ」の min秒〜max秒 2欄・min==max は単一 `seconds` で保存) |
| `"plays"` | `count`(整数≥1、既定1) | 全チャンネル合計の再生回数 |
| `"transitions"` | `count`(整数≥1、既定1) | ステート移行回数 |
| `"cond"` | `when`(判定式リスト) | **変数の判定式(AND)が成立したら終了**。書式は通常イベントの cond と同一(watch/next-cond と共通)。ステートを跨いで再生中に約50ms間隔で常時評価し、成立で再生中の音声を終えてから次イベントへ(グレースフル)。変数宣言がある時のみ選択可 |
| `"states"` | `states`(ステートIDのリスト、1つ以上) | **指定ステートのいずれかが終了したらイベント終了**(そのステートの再生完了後=移行せず終了)。終了ステートを「あがり(accept)」として使える。複数指定可。編集画面ではチェックボックスで選択。 |
| `"none"` | (なし) | **無限**。イベント自身は終わらない。移行条件が成立すればステートは進み続ける。`next` の制限は通常イベントと同じ(なし/選択肢/数値入力/判定式のみ)。編集画面では「無限」 |

### ステート

| 要素 | 型 | 必須 | 取りうる値・既定 |
|---|---|---|---|
| `channels` | オブジェクト | 任意 | 通常イベントと同じ("L"/"C"/"R")。**省略/空 `{}` = 音声なしステート**(下記) |
| `device` | 文字列/オブジェクト | 任意 | 通常イベントと同じ(音声なしステートでは指定不可) |
| `seek_channel` | 文字列 | 任意 | 通常イベントと同じ(ステートごとに指定可。音声なしステートでは指定不可) |
| `transition` | オブジェクト | 任意 | 省略=移行なし(イベント終了条件まで滞在) |

ステート内のチャンネルは `end` 省略で「無限(移行/イベント終了まで)」になる。
全チャンネルが自然終了した場合、移行条件を満たしていれば移行、
満たしていなければイベント終了。

**音声なしステート**: `channels` を省略/空にすると即時通過ノードになる
(意味論・安全機構は音声なしイベント[A']と同じ。イベント内のステート巡回も
連続通過カウンタの対象)。`transition` は**「なし」(省略)/`state_time`
(経過時間)/`cond`/`choice` のみ**使用できる(`channel_count`/
`all_channels`/`channel_end` は対象chが無いので読み込みエラー)。
`choice` は選ばれるまで無音で待機する(下記「選択肢でステート移行」)。
**`state_time` を指定すると、その秒数だけ無音で待ってから移行する
「無音待機ステート」**になる。cond 移行は入場時点で成立していれば移行、
していなければ即座にイベント終了扱いになる(**待っている間に変数を書き換える
実行主体が居ないため、cond で無音待機はできない**)。

### transition(移行条件)

```json
"transition": {
  "when": { ... },
  "to": "S2"                     ←固定移行
}
"to": {"random": ["S1", "S2"]}   ←均等抽選
"to": {"random": ["S1",                              ←重み付き抽選
                  {"to": "S2", "weight": 3},
                  {"to": "S3", "weight": {"var": "w_ext"}}],
       "else": "S1"}
```

**to(移行先)**: 文字列=固定移行。`{"random": [...]}` の各候補は
文字列(重み1)または `{"to": ステートID, "weight": 重み}`。
重みは定数または `{"var": 数値変数}`(**移行が成立した瞬間**の変数値で解決)。
**解決後の重みが0以下の候補は抽選に出ない**(0/負の指定はエラーにしない)。
全候補の重みが0以下のときは `else` のステートへ移行(省略時=**イベント終了**)。
イベントの `next` ランダム分岐と同じ規則で、選択肢の `ops` で
重み変数を書き換えれば「同じステート群のまま抽選プールを変える」ことができる。

**to の判定式(cond)形式**: イベント `next` の cond と同じ書式で、
**変数の値によって移行先ステートを確定分岐**できる。random とは同時指定不可。

```json
"to": {"cond": [{"when": [{"var": "d", "op": "==", "value": 1}], "to": "S1"},
                {"when": [{"var": "d", "op": "==", "value": 2}], "to": "S2"}],
       "else": "Sx"}
```

上から順に評価し、最初に成立した行へ移行する(行内の `when` はAND)。
判定は**移行条件(when)が成立した瞬間**の変数値で行う。
**`else` は省略可**で、省略時(または null)にどの行も成立しないと
**移行しない**(=transitionなしと同じ扱いで再生を続け、イベント終了条件に従う。
イベント版の「else必須・null=再生終了」とは異なるので注意)。
どの行も成立しない間は移行条件そのものが発火を見送るため、条件が成立した
時点で改めて移行する(例: `channel_count` 3回目で d が範囲外なら4回目以降も
判定を続ける)。

**未実行のステートを優先**: `to` のdictにイベントの `next` と同じキーを書ける。

```json
"to": {"random": ["S1", "S2", "S3"],
       "visited": "exclude",
       "when_exhausted": "reset"}
```

- `"visited"`: `"include"`(既定) / `"exclude"`=**このイベント実行中に実行済みの
  ステートを候補から除外**する。履歴は**イベントに入るたびリセット**
  (◀◀での再入も新しい実行として扱う。イベント版は再生全体で保持するが、
  ステートはイベント内部の存在なのでこの作法)。
- `"when_exhausted"`(全候補が実行済みのとき): `"all"`(既定)=除外をやめて
  全候補から抽選 / `"reset"`=履歴から候補ぶんを消して再一巡(ランダムバッグ
  相当) / `{"to": ステートID}`=固定移行(重み・elseは見ない。存在しないIDは
  読み込みエラー)。
- 除外後の候補に対して従来どおり重みを解決し、引ける候補が無ければ
  `else`(省略=イベント終了)。編集画面は「ステート移行先」の下の
  「分岐:」メニュー(**未実行のステートを優先**)+「全候補が実行済みのとき:」。

**when** の形式(min≠maxの場合、ステート再入のたびに閾値を範囲内で抽選):

| type | フィールド | 意味 |
|---|---|---|
| `"state_time"` | `seconds`(固定) または `min_seconds`+`max_seconds`(**channel は不要**) | **このステートに入ってからの経過時間(秒)**。チャンネルを見ないので音声なしステートでも使える。一時停止中は進まない |
| `"channel_count"` | `channel`、`count`(固定) または `min`+`max`(整数≥1) | 対象chの再生回数 |
| `"cond"` | `conds`(判定式リスト) | **変数の判定式(AND)が成立したら移行**。`channel`/閾値は不要。書式は watch/next-cond/イベント終了cond と同一。再生中に約50ms間隔で常時評価し、成立で移行(グレースフル)。`to` は固定/`{"random":[...]}` 抽選(重み・else可)のどちらも可。変数宣言がある時のみ選択可 |
| `"all_channels"` | (なし) | **ステート内の全チャンネルが自然終了したら移行**(イベントの「全チャンネル終了」と同じ)。`channel`/閾値不要。全chが有限(無限以外)である必要がある(1つでも無限だと発火しない)。編集画面ではこの選択時、有効chに無限があると保存エラー。 |
| `"channel_end"` | `channel`(そのステートに存在するchID) | **指定チャンネルが終了したら移行**(イベントの「指定チャンネル終了」と同じ)。他chは再生中の音声を終えてから停止(ワインドダウン)。対象chは有限(無限以外)である必要がある(他chは無限でも可)。 |

| `"choice"` | (なし・`to` も書かない) | **選択肢でステート移行**。選ばれた選択肢(またはタイムアウトの既定)で次のステート、または遷移先イベントが決まる。書式は下記 |

> **`"channel_time"` は廃止した**(`state_time` と役割が重複するため)。
> 読み込み時にエラーになる。旧シナリオは `state_time` へ書き換えるか、
> 編集画面で開いて保存し直す(「経過時間」へ読み替えて開く)。

### 選択肢でステート移行

```json
"transition": {
  "when": {"type": "choice"},
  "choice": [
    {"label": "つづける", "to": "S2"},
    {"label": "やめる",   "to": {"event": "outro"}, "ops": [{"add": "quit", "value": 1}]}
  ],
  "timeout": {"seconds": 20},
  "on_timeout": [...],
  "default": {"to": "S3"},            ←省略=先頭 / "random" / {"to": ステートID} / {"to": {"event": イベントID}}
  "skip": "stay",
  "show": "start"
}
```

`to` は書かない(書くと読み込みエラー)。`choice`/`timeout`/`on_timeout`/
`default`/`skip` はイベントの `next` 選択肢形式と同じ意味(下記「選択肢形式」)。
違いは次の2点:

- **行き先**: 各選択肢の `to` と `default.to` は **ステートID(文字列)** または
  **`{"event": "イベントID"}`**(イベントを終了してそのイベントへ飛ぶ)。
  存在しないIDは読み込みエラー。
- **`show` の基準はステート**: `"start"`=ステート開始時 / `"end"`(既定)=
  ステート内の全チャンネルが終了した時 / `{"seconds": N}`=ステート開始からN秒後。

**動作仕様**:
- 選択またはタイムアウトで**即座に**移行する(再生中の音声は打ち切り。
  イベント側の選択肢の「再生中に選択」と同じ)。
- **全チャンネルが終わっても選ばれるまで無音で待機**する(そのステートは
  終わらない)。ただし**イベント終了条件(合計時間・変数条件など)が成立したら
  選択肢を待たずに閉じてイベントを終了**する(監視 watch の成立も同じ)。
- ステートを抜けるときステートの `on_end` が発火する。イベント宛てのときは
  イベントの `on_end` も発火してから遷移する(自然終了扱い。イベント側の
  選択肢/数値入力の待機は挟まない)。
- ▶▶は**従来どおりイベント全体のスキップ**(選択肢の既定へは進まない。
  タイムアウトの既定は `timeout` で表現する)。`skip: "stay"` のときは
  **選択肢の表示中の▶▶を無効**にする(音声も止めない。ログに残る)。◀◀は従来どおり。
- イベント終了条件「ステート移行N回」の回数に数える。「指定ステートが終了」の
  対象ステートなら、選択で抜けた時点でイベント終了(既存の規則)。
- **イベント側の選択肢/数値入力との併用**: 表示枠は1つなので同時には出ない。
  ステート移行に選択肢があるイベントでは、イベント側の `next` の選択肢/数値入力は
  **`show: "end"`(イベント終了条件の達成時)のみ**指定できる(それ以外は
  読み込みエラー。終了条件が「無限」のイベントでは達成時が来ないため併用不可)。
- 編集画面: ステート移行コンボ「選択肢でステート移行」。行ごとに
  「ステート/イベント」を切り替えて行き先を選ぶ。

## BGM— イベント/ステートをまたぐ連続再生

トップレベル `"bgm_enabled": true` のシナリオでは、各ノード(通常イベント/
ステート形式の各ステート)に **`"bgm"`** を指定できる。BGMは L/C/R とは
独立した専用の再生系で鳴り、**イベント境界・ステート移行の全音声停止の
対象外**なので、すごろくの全マスや戦闘イベントのステート往復の間、同じ
曲を途切れず流し続けられる。

```json
"event1": {
  "channels": { "C": { "items": ["voice.wav"] } },
  "bgm": {
    "items": ["battle1.wav",
              {"audio": "battle2.wav", "pan": {"left": 0.4, "right": 0.4}}],
    "order": "random",
    "pan": {"left": 0.6, "right": 0.6}
  },
  "next": "event2"
}
```

| キー | 値 | 説明 |
|---|---|---|
| (キー省略) | — | **前のBGMを引き継ぐ**(既定)。直前まで鳴っていたBGMがそのまま鳴り続ける(何も鳴っていなければ無音のまま)。開始イベントに1回指定すれば全ノードへ行き渡る |
| `"off": true` | 真偽値 | **BGM停止**。300msのフェードアウトで止める。`false` は不可(引き継ぐ場合はキーごと省略する) |
| `items` | 配列(1件以上) | **BGM指定**。`"パス"` か `{"audio": "パス", "pan": {...}}`。wav/mp3。0件はエラー。**入場のたび先頭から再生し直す**(同じ構成でもリスタート。続けたいノードは「引き継ぐ」=キー省略にする) |
| `order` | `"sequential"` / `"random"` | 再生順(省略=`sequential`)。順番=リストを周回。ランダム=袋方式(1周分をシャッフルして出し切り、周回の境目で同じ曲が連続しないよう配慮) |
| `pan` | `{"left": 0〜1, "right": 0〜1}` | BGM既定パン(アイテム個別 `pan` が優先)。左右を同じ値で下げれば実質の音量調整になる(声より小さくしたいときに) |

**性質・制約**(通常チャンネルとの違い):

- 常に**無限ループ**(終了条件・インターバルは持たない)。1曲なら
  ミキサー任せのギャップレスループ、複数曲は予約(queue)方式でほぼ
  ギャップレスに繋ぐ。
- **デバイス連動の対象外**(スクリプトの紐づけ不可)。シークバー追従・
  再生回数(plays)・`all_channels`/`channel_end` の判定にも数えない。
  シーク操作でもBGMは巻き戻らない。
- アイテムは**ファイル+個別パンのみ**(重み・区間・変数操作は不可)。
- 一時停止/再開・マスター音量には追従する。停止・シナリオ終了・BGMの
  差し替え時は300msフェードアウト。
- ステート形式では `bgm` は**各ステート**に書く(イベント直下はエラー)。
- 音声なし(チャンネルなし)ノードにも書けるので、「BGM切替専用の
  サイレントノード」として分岐と組み合わせられる。
- `bgm_enabled` が `false`(省略)なら、`bgm` 定義が残っていても再生・
  表示とも無効(定義は保持。`true` へ戻せば復活)。

再生ログには、ノード入場でBGMが変化したときだけ
`♪ BGM開始: ファイル名 (順番・2曲)` / `♪ BGM停止` が記録される
(「引き継ぐ」は記録しない)。

## 動画(video)— 外部mpv連携

チャンネルの `items` に **動画アイテム**を入れると、再生時に外部の **mpv** が
自動起動して動画を再生する(mpvのパスは接続タブで設定。**mpvはオプショナル**=
動画を使わないシナリオでは不要。動画を含むシナリオの再生時に mpv が無ければエラー)。

**フェーズ3-Bで、動画は「イベント/ステート直下の `video`」から
「動画アイテムを持つチャンネル」へ移った。**これにより動画は音声chと同じ枠組み
(順番/ランダム再生・終了条件・重み・インターバル)にそのまま乗る。

```json
"channels": {
  "C": {"mode": "random",
        "items": [{"video": "scene1.mp4"},
                  {"video": {"file": "long.mp4", "start": 60, "end": 90},
                   "tracks": [{"type": "linear", "funscript": "..."}]}],
        "end": {"type": "duration", "seconds": 120}},
  "L": {"items": ["voice01.wav", "voice02.wav"], "end": {"type": "none"}}
}
```

| 要素 | 型 | 必須 | 意味 |
|---|---|---|---|
| `video` | 文字列 or オブジェクト | 必須 | 動画ファイル。文字列=最小形。オブジェクトは `file`(旧形式の `start`/`end` も読めるが、保存時は `range` へ移る) |
| `video.file` | 文字列 | 必須 | 動画ファイル(存在チェックあり。拡張子は制限しない=mpv任せ) |
| `video.start` | 数値(秒) | 任意 | **区間の開始秒**(既定0=動画の先頭)。0以上 |
| `video.end` | 数値(秒) | 任意 | **区間の終了秒**(既定=動画の末尾)。`start` より後 |
| `tracks` / `funscript` | ― | 任意 | 動画クロックに完全同期するトラック(音声アイテムと同じ規則・rotate系CSV可)。未指定は動画ファイル名の命名ルールで自動紐づけ |
| `weight` | 数値 or `{"var":..}` | 任意 | ランダム再生chでの抽選の重み(音声アイテムと同じ変数参照・全0時のチャンネル終了の規則) |

`pan` は動画アイテムでは無視される(動画の音声は mpv 側の管理)。

### 動画チャンネルの制約

- **1つのチャンネルには音声・動画・スクリプトのどれか1種類だけ**(混在不可)。
  同じアイテムに `audio` と `video` を併記することもできない。
- **動画を置けるチャンネルは1つだけ**(2ch以上に動画があると読み込みエラー)。
  「動画は同時に最大1本」という合意事項をチャンネル形式でも維持する。
- 動画chがあると **シークバーは動画chに固定**される(`seek_channel` 指定不可)。
- 動画chの `device` 既定は**担当なし**(全種別が動画側)。チャンネルに担当させる
  種別は `device` で明示する。
  - 動画のトラックがある種別は、`device` で**動画チャンネル自身を
    指してよい**(`"device": {"linear": "C"}`。C=動画ch)。音声チャンネルと
    同じ書き方で「どこが駆動源か」を明示できる。**省略(担当なし)も引き続き
    有効**なので、旧シナリオはそのまま読める。編集画面は動画のトラックが
    ある種別の**既定を動画chのID**にして書き出す。
  - 動画のトラックがある種別を**別のチャンネルへ付け替えられる**
    (`"device": {"linear": "L"}`)。このとき**動画側の linear トラックは
    再生されず**、チャンネルLが linear の駆動源になる。**「動画とは別のリズムで
    デバイスを動かす」**用途のための仕様。
  - **判定のまとめ**: 動画のトラックは「**その種別の担当が別のチャンネルに
    あるときだけ鳴らない**」。担当が指定されていない(=省略)種別は
    従来どおり動画が鳴らす。したがって駆動源は常に1つ。
- 動画chの終了条件が「無限」(`{"type":"none"}`)のときは、イベント側にも終了条件
  (duration/cond/channel など)が必要(そのままでは終わらないため)。

### 終了条件とループ

動画chの終了条件は音声chと同じ(`once` / `repeat` / `duration` / `plays` / `none`)。

- **動画が終わったら次のイベントへ**: 動画chの `end` を `{"type":"once"}` にし、
  イベントの `end` を `{"type":"channel","channel":"<動画ch>"}` にする。
- **短いループアニメを繰り返しながら待つ**: 動画を **1本だけ**入れて `end` を
  `duration`(N秒)や `none`(無限)にする。**アイテムが1本かつ終了条件が時間/無限の
  ときは mpv 側でループする**(区間なし=`loop-file` / 区間あり=`ab-loop-a`/`ab-loop-b`)
  ため、継ぎ目が出ない。複数本のときは RVP がアイテムを切り替える。
- 旧形式の `"loop": true` は読み込み時に「チャンネル終了条件=無限」へ変換される。

### 区間指定(動画に固有の点)

区間指定そのものは音声・スクリプトと共通の仕様になった。
書式と基本の考え方は「[区間指定(range)](#区間指定range)」を参照。
ここでは動画に固有の点だけを挙げる。

- **`end` への到達=そのアイテムの終わり**。到達時点で mpv は一時停止する
  (区間終端では `eof` が立たないため明示的に止める)。
- **同じ動画ファイルが続く場合は読み直さない**: 直前のアイテム/ステートと
  `file` が同じなら `loadfile` せず区間の先頭へシークするだけで繋ぐため、
  切り替え時に画面が黒くならない。同じ長尺動画の別区間を候補として並べて
  ランダム抽選する使い方でも、つなぎが滑らかになる。
- **動画だけは長さの事前検証ができない**(mpv がないと判定できないため)。
  音声・スクリプトは読み込み時にエラーになるが、動画は
  **`start` が動画の実長を超えているとそのアイテムは一瞬で終わる**
  (シーク先が終端の外になり mpv が即座に「再生し終わった」状態になる)。
  そのため **再生時に④変数・イベントログへ警告行を出す**。
  「動画が一瞬で切り替わる」ときはまずログの ⚠ 行を確認すること。
- `end` が動画の実長を超える指定もエラーにしない(再生時は動画の末尾で終わる)。

### 旧形式からの自動移行

イベント/ステート直下の `"video"` は**読み込み時に動画チャンネルへ自動移行**される
。編集画面で開いて保存すると新形式で書き出される。

- 空きチャンネル(**C→L→R** の優先順)へ「動画アイテム1つのチャンネル」として入る。
  L/C/R がすべて埋まっている場合は移行できないため、明確なエラーになる
  (どれか1チャンネルを空ける必要がある)。
- `loop` なし → チャンネル終了条件 `{"type":"once"}`(1周で終了)。
  `loop` あり → `{"type":"none"}`(無限)。
- 通常イベントで `end` 未指定かつ非ループだった場合は、旧挙動「動画が終わったら
  イベント終了」を保つため `end` に `{"type":"channel","channel":"<動画ch>"}` を補う。

### イベント終了条件との関係(よくある誤解)

イベントの終了条件(合計時間・変数条件など)は**上限**であって「最低限その時間
続ける」という意味ではない。**全チャンネルが先に再生し終えると、そこでイベントは
終了する**(動画に限らず音声chでも同じ)。

- 例: 動画chが「1周で終了」で中身が合計10秒なら、イベント終了条件を
  「合計80〜100秒」にしても**10秒でイベントが終わる**。
- 指定時間まで続けたいときは**チャンネルの終了条件を「無限(移行/終了まで)」**に
  する。複数の動画なら順番/ランダムで回り続けながらイベント終了条件を待つ。

### 再生ログ

再生タブの⑤変数・イベントログに、**アプリがシナリオをどう解釈したか**と
**なぜ次へ進んだか**が記録される。JSONを読まずに設定の食い違いを見つけられる。

```
イベント: event1  [終了条件: 合計80〜100秒(今回87秒)]
  ・C: 音声 / ランダム再生 / 無限 / 12アイテム / 間隔0.5〜1秒
  ・L: 動画 / 順番に再生 / 無限 / 2アイテム  (シークバー追従)
    ▶ L: 01_予告公開時.mp4 20〜25s
    ✎ hp: 100 → 80 (加算 -20) [アイテム再生時]
    ☰ 選択肢を表示: 1)進む 2)戻る (制限20秒 / 既定=1)進む)
  → イベント終了: 合計時間 87秒に到達
  → 選択肢を選択: 2)戻る → e_back
```

| 行 | 内容 |
|---|---|
| `イベント: <ID>  [終了条件: …]` | 入場時に**解決した**終了条件(範囲抽選は今回の値も) |
| `  ステート: <ID>  [移行条件: …]` | ステート形式のみ |
| `  ・<ch>: …` | そのチャンネルの解釈後の設定(種別/再生方式/終了条件/アイテム数/インターバル/担当デバイス/シークバー追従) |
| `    ▶ <ch>: <ファイル名> <区間>` | 再生を開始したアイテム(音声・動画・スクリプト) |
| `    ✎ <名前>: <前> → <後> (<種別> <値>) [<発火場所>]` | **変数操作** |
| `    ☰ …` | **選択肢/数値入力の表示** |
| `    ⚠ …` | 警告(区間が動画長を超える・音声の読み込み失敗など) |
| `  → …` | **なぜ次へ進んだか**(終了条件・移行条件・選択肢・監視の成立理由) |

「思ったより早く次のイベントへ進む」ときは `  → ` 行を見れば理由が分かる
(例: `イベント終了: 全チャンネルが終了` なら、イベントの終了条件より先に
チャンネルが終わっている)。

#### 変数操作の行

変数が変わるたびに1行残る。**値が変わらなかった場合も残す**(操作されたこと
自体が分かるように)。

- 種別は `代入` / `加算`(符号つき `+5` / `-20`) / `乗算` / `乱数 lo〜hi`
  (**変数参照を解決したあとの値**)。
- 他の変数を参照した操作は `←変数名` が付く(`roll` は `←lo,hi` のように2つ)。
- 宣言の `min`/`max` で丸められたら `(上限で丸め)` / `(下限で丸め)` が付く。
- 発火場所は9種: `イベント開始時` / `イベント終了時` / `ステート開始時` /
  `ステート終了時` / `アイテム再生時` / `アイテム完了時` / `選択時` /
  `タイムアウト時` / `数値入力`。

#### 選択肢・数値入力の行

- 表示時: `☰ 選択肢を表示: 1)進む 2)戻る (制限20秒 / 既定=1)進む)`。
  制限時間が無ければ「制限」は出ない。既定は `default` の指定に応じて
  `1)進む` / `ランダム` / `<イベントID>`。
- 決定時: `→ 選択肢を選択: 2)戻る → e_back`(**押した番号とラベル**)。
  タイムアウトは `選択肢がタイムアウト:`、▶▶は `選択肢を手動でスキップ(▶▶):`
  で、いずれも抽選後の遷移先から候補を逆引きして番号とラベルを出す。
- 数値入力: `☰ 数値入力を表示: n (範囲 1〜10) → e_next` と
  `→ 数値入力を確定: 7 → e_next`。入力値の変数セットは `✎` の行にも出る。

### その他の仕様

- **同期**: 動画アイテムのトラックは mpv の再生位置に従属し、**一時停止・シークに
  完全追従**する。一時停止・シークは RVP⇄mpv の双方向で同期する。
- **チャンネル併用可**: 音声ch(ボイス重ね)・スクリプト専用chを併用できる。
- ステート形式では各ステートの `channels` に動画chを置く(イベント直下の `video` は
  旧形式でもエラー)。`items` 直書き形式と旧 `video` の併用も不可。
- **mpvの挙動(合意仕様)**: 動画chのあるイベント/ステートへの入場時に遅延起動し、
  以後プロセスを維持して `loadfile` で差し替える。動画の音声は mpv からそのまま出る
  (RVPは音量に触れない)。再生停止・動画の無いイベントへの遷移では**一時停止して残り**、
  mpv が閉じるのは RVP終了時のみ。◀◀での再入場は頭から再生し直す。
- **編集UI**: チャンネルの「＋ 動画を追加」で動画アイテムを追加する(複数選択可)。
  アイテムの「区間:」欄で start/end を秒で指定できる(空欄=動画全体)。
  「自動(ルールで紐づけ)/手動」で `tracks` を編集できる
  (種別+funscript/CSVのトラック行。最大4本・種別重複不可。0本=`"funscript": null`)。
  動画ファイルはチャンネル枠へのD&Dでも追加でき、動画アイテムへ funscript/CSV を
  落とすとその種別の動画トラックに紐づく。動画トラックが担当する種別は、チャンネルの
  デバイス担当メニューが自動で「なし」に固定される(種別一意性の先回り防止)。
  混在禁止・動画ch1つの制約も追加ボタンの有効/無効で先回り防止される。
- **保存時の素材コピー**: 「素材をコピーして相対パスで保存」の対象に動画を含む
  (動画と同名系の funscript/CSV も同伴コピー)。確認画面にコピー量の合計サイズを表示する。

## next(遷移)

イベント(通常/ステート形式とも)の `next` は6形式。
オブジェクト形式(random/choice/cond/input)は1つのnextにつき1形式のみ。

| 書き方 | 意味 |
|---|---|
| `null` / 省略 | シナリオ終了 |
| `"eventB"` | 単純遷移(後方互換) |
| `{"random": [...], ...}` | 重み付きランダム分岐(下記) |
| `{"choice": [...], ...}` | 選択肢(ボタンで遷移先を選ぶ、下記) |
| `{"cond": [...], "else": ...}` | 変数分岐(下記) |
| `{"input": {...}, ...}` | 数値入力(入力値を変数へセットして遷移、下記) |

### advance(すごろく)

イベントに `"advance": N`(1以上の整数 or `{"var": 変数名}`)を指定すると、
そのイベントを抜けるとき **next の解決を合計N回繰り返し、N番目の着地
イベントだけを再生する**(サイコロの出目でN歩進むすごろく移動)。

```json
{"roll": {"on_start": [{"roll": "d", "min": 1, "max": 6}],
          "advance": {"var": "d"}, "next": "masu1", ...},
 "masu1": {"next": "masu2", ...}, "masu2": {"next": "masu3", ...}}
```

- **通過**したイベントは再生されず、`on_start`/`on_end`・監視(watch)も
  発火しない(純粋な経路解決のみ)。通過先の決定はそのイベント自身の
  next(単純/random の候補と重み/cond)に依拠し、重み変数・else・
  未実行優先(random_bag)もそのイベントの規則どおりに働く。
  通過は「実行済み」(`visited: "exclude"` の対象)には数えない。
- 通過中に**遷移の無いイベント(ゴール)**へ到達したら、そこに着地して
  再生し、シナリオが終わる(あがり。残り歩数は捨てる)。抽選の結果が
  行き先なし(重み全0でelse無し等)になったときは、その場であがり扱いで
  終了する(通過中のイベントは再生しない)。
- 通過中に**選択肢/数値入力**のイベントへ当たったら、そこに止まる
  (残り歩数は捨てて着地・再生)。
- 変数の歩数は遷移時点の現在値で解決し、0以下は1に丸め、上限999。
- advance は **next規則による遷移にだけ**適用される。選択肢/数値入力の
  確定・監視(watch)による遷移・イベントバック(◀◀)には適用されない
  (選択肢/数値入力の next を持つイベントへの指定は読み込みエラー)。
- 編集画面では「次のイベント」の下の**「すごろく」チェック**で指定する
  (ONで「進む歩数」欄=数値または変数)。

```json
"next": {
  "random": ["B", {"to": "C", "weight": 3}, {"to": "D", "weight": {"var": "wD"}}],
  "visited": "exclude",
  "when_exhausted": "reset",
  "else": "no_move"
}
```

| 要素 | 型 | 必須 | 取りうる値・既定 |
|---|---|---|---|
| `random` | 配列 | 必須 | 候補。文字列 `"B"`(weight=1) または `{"to":"B","weight":3}`。**weightは数値定数 or `{"var":"名前"}`(数値変数参照)**。**解決した重みが0以下の候補は「出さない」(抽選対象外)**=変数で出し分け・イカサマダイス・条件付きで遷移可能なマス等に使える。重みに比例した確率で抽選 |
| `visited` | 文字列 | 任意 | `"include"`(既定) / `"exclude"`=このシナリオ再生中に**実行済みのイベントを候補から除外**して抽選 |
| `when_exhausted` | 文字列/オブジェクト | 任意 | `visited: "exclude"` で全候補が実行済みになったときの挙動。`"all"`(既定)=除外をやめて全候補から抽選 / `"reset"`=候補分の訪問履歴をリセットして再一巡 / `{"to": "id"}`=指定イベントへ |
| `else` | 文字列 | 任意 | **解決後の重みが全候補0以下=出せる候補が無いときの遷移先**(イベントID)。省略時は遷移なし(シナリオ終了)。編集画面では「重みが全て0のとき:」で選択(「(終了)」=省略) |

- 訪問履歴は再生開始でリセットされる。◀◀で戻って再実行した場合もカウントされる。
- 収束(複数イベントのnextが同じイベントを指す)とループ(末尾→先頭)は単純遷移の組み合わせで表現できる。
- 例: 「初回だけB、2回目以降はC」= `{"random": ["B"], "visited": "exclude", "when_exhausted": {"to": "C"}}`
- 例(条件付きサイコロ): 重みを変数にして、条件を満たすまで特定の面を0にして出さない。すごろくの「特定の状態でだけ進めるマス」等。

### 選択肢形式

```json
"next": {
  "choice": [
    {"label": "洞窟へ進む", "to": "cave"},
    {"label": "引き返す", "to": "camp"},
    {"label": "降参する", "to": "surrender"}
  ],
  "timeout": {"seconds": 30},
  "default": {"to": "punish"},
  "show": "end"
}
```

| 要素 | 型 | 必須 | 取りうる値・既定 |
|---|---|---|---|
| `choice` | 配列 | 必須 | 1〜9件。各要素は `{"label": ボタン表示テキスト(空不可), "to": 遷移先イベントID}` |
| `timeout` | オブジェクト | 任意 | 省略=無制限(選択されるまで待つ)。指定時は `seconds`(>0)。到達時は**デフォルト遷移先**へ |
| `default` | 文字列/オブジェクト | 任意 | **デフォルト遷移先**(タイムアウト時・▶▶スキップ時共通の行き先)。省略=先頭の選択肢 / `"random"`=選択肢から等確率で抽選 / `{"to": "id"}`=指定イベントへ(**選択肢に無いイベントも可**) |
| `show` | 文字列/オブジェクト | 任意 | 選択肢を表示するタイミング。`"end"`(既定)=イベント終了条件の達成時 / `"start"`=イベント開始時 / `{"seconds"}`=イベント開始から指定時間後 |
| `skip` | 文字列 | 任意 | **▶▶スキップの扱い**。`"default"`(既定)=デフォルト遷移先へ進む(従来どおり) / `"stay"`=**選択必須**。▶▶では飛ばさず、選択されるまで待機する(何度押しても進行しない)。それ以外の値は読み込みエラー |

**動作仕様**:
- 再生UIにボタンが表示される(1〜9個。1=1x1 / 2=1x2 / 3〜4=2x2 / 5〜6=2x3 / 7〜9=3x3、端数は右下から欠ける)。
- **選択またはタイムアウトで確定した時点で即遷移**する(イベント再生中なら▶▶スキップと同様に中断)。
- イベントが選択前に自然終了した場合は、無音のまま選択を待つ(タイムリミットがあれば継続してカウント)。
- タイムリミットのカウントは選択肢が表示された時点から始まり、一時停止中は進まない。
- 選択肢表示中の▶▶スキップは、選択を経ずにデフォルト遷移先へ進む。
- **`skip: "stay"`(選択必須)** のときは▶▶で飛ばせない: 待機中の▶▶は無視され(ログに残る)、**音声再生中の▶▶は音声を打ち切って選択肢の待機へ進むだけ**(自然終了扱い=`on_end` は発火する)。◀◀は従来どおり効く。タイムリミット併用時の**タイムアウト遷移は従来どおり進む**(「手動では飛ばせないが時間切れは進む」は両立する)。
- 選択肢表示中の◀◀は、実行履歴に従って一つ前のイベントへ戻る(選択肢は影響しない)。
- `default` の `{"to"}` に選択肢外のイベントを指定すると、「選択放棄への罰」や「降参せず耐久成功」のような、選択しなかったこと自体への反応イベントを作れる。
- 旧形式 `"timeout": {..., "to": "id"}` も引き続き読み込める(`default: {"to": "id"}` と同義)。`default` との併記はエラー。編集UIで保存すると `default` 形式へ移行される。

**変数との連携**(詳細は「変数」章):
- 各選択肢に `"ops": [...]` を書くと、**その選択肢が選ばれた時のみ**変数操作が実行される(遷移の直前)。
- choiceオブジェクト直下に `"on_timeout": [...]` を書くと、**タイムアウト確定時のみ**実行される(タイムリミット未設定なら書かれていても発火しない)。
- ▶▶スキップでは `ops` も `on_timeout` も発火しない(スキップは中立操作)。

### 変数分岐形式(cond)

```json
"next": {
  "cond": [
    {"when": [{"var": "success", "op": ">=", "value": 3}], "to": "perfect_end"},
    {"when": [{"var": "success", "op": ">=", "value": 1},
              {"var": "HP", "op": ">", "value": 0}], "to": "normal_end"}
  ],
  "else": "bad_end"
}
```

| 要素 | 型 | 必須 | 取りうる値・既定 |
|---|---|---|---|
| `cond` | 配列 | 必須 | 1件以上の分岐行。各行は `{"when": [判定式...], "to": 遷移先}`。**上から順に評価し、最初に成立した行へ遷移** |
| `else` | 文字列/null | **必須** | どの行も成立しなかった時の遷移先。`null`=再生終了。書き忘れ防止のためキー自体の省略はエラー |

- `when` のリストは **AND**(全て成立で行が成立)。ORは行を分けて表現する。
- 判定式は `{"var": 変数名, "op": 演算子, "value": 右辺}`。演算子は `==` `!=` `>=` `<=` `>` `<` の6種。右辺は定数(数値/文字列)または `{"var": "他変数"}`。
- 文字列変数は `==` `!=` のみ。左右の型が一致しない指定は読み込みエラー。
- random / choice / cond / input は同時に指定できない(1つのnextにつき1形式)。
- 編集画面では遷移方法「変数分岐」として編集できる(変数が1つ以上宣言されているときのみメニューに表示される)。

### 数値入力形式(input)

```json
"next": {
  "input": {
    "var": "len_min",
    "label": "再生時間(分)を入力",
    "to": "main_play",
    "min": 1,
    "max": 30
  },
  "show": "start"
}
```

| 要素 | 型 | 必須 | 取りうる値・既定 |
|---|---|---|---|
| `input.var` | 文字列 | 必須 | セット先の**数値変数**名(vars宣言必須。文字列変数はエラー) |
| `input.to` | 文字列 | 必須 | 遷移先イベントID |
| `input.label` | 文字列 | 任意 | 入力欄の見出しテキスト。省略時は「数値を入力してください」 |
| `input.min` / `input.max` | 数値 | 任意 | 受理する入力範囲。**省略時は変数宣言の min/max**(宣言にも無ければ制限なし)。宣言範囲の外へ広げる指定は読み込みエラー(min>maxも同様) |
| `show` | 文字列/オブジェクト | 任意 | 入力欄を表示するタイミング(choiceと同じ)。`"end"`(既定)=イベント終了条件の達成時 / `"start"`=イベント開始時 / `{"seconds"}`=イベント開始から指定時間後 |
| `skip` | 文字列 | 任意 | **▶▶スキップの扱い**。`"default"`(既定)=変数を変えずに `to` へ進む(従来どおり) / `"stay"`=**入力必須**。▶▶では飛ばさず、入力されるまで待機する(音声再生中の▶▶は音声を打ち切って入力待ちへ進むだけ)。それ以外の値は読み込みエラー。編集画面で新規に「数値入力」を選ぶと **ON(stay)が既定**で書かれる |

**動作仕様**:
- 再生UIに数値入力欄と「決定」ボタンのカードが表示される(Enterキーでも決定)。
- 非数値・範囲外の入力は決定時に赤いエラー表示となり再入力を求める(受理されるまで遷移しない)。入力範囲は見出しの隣に表示される。
- 決定した値は `set` 操作として変数へ代入され(整数値は整数として格納、宣言のmin/maxクランプも適用)、**確定した時点で即遷移**する(イベント再生中なら▶▶スキップと同様に中断)。監視(watch)の評価も通常の変数操作と同様に働き、watchが発火した場合は入力の遷移より優先される。
- イベントが決定前に自然終了した場合は、無音のまま入力を待つ(タイムリミットは無し。決定まで無制限に待つ)。
- 入力表示中の▶▶スキップは、**変数を変更せずに** `to` へ進む(中立操作)。`skip: "stay"` のときは無視される(ログに残る)。
- 入力表示中の◀◀は、実行履歴に従って一つ前のイベントへ戻る(変数は変更されない)。
- 一時停止中も入力・決定は可能。決定すると遷移先イベントが一時停止状態で始まり、再生ボタンで開始する(選択肢と同じ)。
- 用途例: 「何分再生するか」を入力させ、次イベントの `end`(duration)に `{"var": ...}` で埋め込む(下記「終了条件・transitionへの変数埋め込み」)。
- 編集画面では遷移方法「数値入力」として編集できる(変数が1つ以上宣言されているときのみメニューに表示される)。

## 変数

イベント間で状態(成功回数・HPなど)を記録・参照する仕組み。

```json
{
  "title": "3問クイズ",
  "vars": {
    "success": 0,
    "grade": "none",
    "HP": {"init": 3, "min": 0, "max": 5}
  },
  ...
}
```

**宣言(トップレベル `vars`)**:

| 形式 | 意味 |
|---|---|
| `"名前": 数値` / `"名前": "文字列"` | 初期値のみの宣言。型は初期値で確定する |
| `"名前": {"init": 3, "min": 0, "max": 5}` | min/max付き宣言(数値のみ)。set/addを問わず**すべての操作結果がこの範囲にクランプ**される |

- 変数は**使用前の宣言が必須**。未宣言の変数を操作・参照するシナリオは読み込みエラーになる(typo検出のため)。
- 変数は**再生開始のたびに初期値へリセット**される。
- **◀◀/▶▶で巻き戻らない**(体験済みの結果はリセットされない)。◀◀でイベントを再実行すると `on_start` 等は再度発火する。
- 再生タブに現在値が常時表示される(vars宣言のあるシナリオのみ)。
- **編集画面**: ツールバーの「変数…」ボタンで宣言と監視(watch)の管理ダイアログが開く。変数が1つも宣言されていない間は、編集画面に変数関連のUI(変数操作ボタン・変数分岐・数値入力・変数指定トグル)は一切表示されない。変数操作(on_start/on_end/on_play/on_complete/ops/on_timeout)は各所の「変数操作(n)」ボタンからポップアップで編集する。変数名の変更・削除は参照箇所を自動修正しない(保存時の検証でエラーとして検出される)。

**操作(op)の形式** — 常にリストで書き、上から順に実行される:

```json
[{"set": "grade", "value": "good"},
 {"set": "HP", "value": {"var": "max_hp"}},
 {"add": "success", "value": 1},
 {"add": "HP", "value": -1},
 {"mul": "all_yes", "value": {"var": "flag"}},
 {"roll": "dice", "min": 1, "max": 6},
 {"roll": "dice", "min": {"var": "lo"}, "max": {"var": "faces"}},
 {"eval": "hit", "when": {"var": "dice", "op": ">=", "value": 5}},
 {"eval": "same", "when": {"var": "a", "op": "==", "value": {"var": "b"}}}]
```

| 種類 | 意味 |
|---|---|
| `{"set": 変数, "value": 右辺}` | 代入。右辺は定数(型一致必須)または `{"var": 他変数}`(同型) |
| `{"add": 変数, "value": 右辺}` | 加算(負数で減算)。数値変数のみ。右辺は数値定数または数値変数 |
| `{"mul": 変数, "value": 右辺}` | **乗算**。数値変数のみ。右辺は数値定数または数値変数。結果は宣言のmin/maxでクランプ。例=**「複数の選択肢がすべてyesのときだけ」の判定**: フラグ変数(init 1)へ各選択肢の ops で yes=×1 / no=×0 を掛けていくと、最後まで1が残る=全yes。そのままcond分岐・ステート移行重み・アイテム重みに使える |
| `{"roll": 変数, "min": a, "max": b}` | 乱数代入。数値変数へ **[a, b] の一様乱数(整数・両端含む)** を代入。`min`/`max` は数値定数または `{"var": 他変数}`(可変レンジ=面数の変更に使える)。`min>max` は自動で入替。結果は宣言のmin/maxでクランプ。例=サイコロ、ランダムイベント抽選 |
| `{"eval": 変数, "when": {"var","op","value"}}` | **条件式**。`when` の判定式1件(書式は cond の条件と同じ・`value` は定数 or `{"var": 他変数}`)を評価し、**成立=1 / 不成立=0** を数値変数へ代入。判定対象(`when` の `var`)は文字列変数も可。**結果は宣言のmin/maxでクランプされる**ので、フラグ変数は min/max 無しか 0〜1 で宣言するとよい。ANDが必要なときは =75 の mul 連鎖(`x=(a==1)` → `y=(b>2)` → `x×=y`)で表現する。例=すごろくで「出目が5以上か」をフラグ化し、cond分岐・重み変数・advance の材料にする |

**操作の配置場所(発火タイミング)**:

| 場所 | キー | 発火タイミング |
|---|---|---|
| イベント直下 | `on_start` | イベントに入った時(◀◀での再実行でも発火) |
| イベント直下 | `on_end` | イベントが**自然終了**する時(停止・▶▶/◀◀では発火しない) |
| ステート直下 | `on_start` | ステートに入った時(再入のたびに発火) |
| ステート直下 | `on_end` | ステートを**自然に抜ける時**(移行/イベント終了。停止・▶▶/◀◀では発火しない) |
| アイテム | `on_play` | そのアイテムの再生開始時 |
| アイテム | `on_complete` | 最後まで自然に再生された時のみ(中断では発火しない) |
| 選択肢エントリ | `ops` | その選択肢が選ばれた時のみ |
| choice直下 | `on_timeout` | タイムアウト確定時のみ(タイムリミット未設定なら発火しない) |
| next直下 | `input` | 数値入力の決定時に入力値を代入(上記「数値入力形式」) |

**参照場所**: `next` の変数分岐形式(cond)、下記の監視(watch)、
および終了条件・transitionの数値欄への埋め込み(下記)。

### 終了条件・transitionへの変数埋め込み

数値を書ける次の欄には、数値の代わりに `{"var": "変数名"}` を書ける(数値変数のみ)。

| 場所 | 対象欄 |
|---|---|
| チャンネルの `end` | `count`(repeat/plays) / `seconds`(duration) |
| イベントの `end`(単一ステート・ステート形式とも) | `count`(plays/transitions) / `seconds`(duration) |
| ステートの `transition.when` | `seconds` / `min_seconds` / `max_seconds`(state_time)、`count` / `min` / `max`(channel_count) |

```json
"end": {"type": "duration", "seconds": {"var": "len_sec"}},
"transition": {"when": {"type": "channel_count", "channel": "C",
                        "count": {"var": "k"}}, "to": "S2"}
```

**動作仕様**:
- **解決タイミング**: イベントの `end` はイベントに入った時(`on_start` 適用後)、チャンネルの `end` と `transition` はステートに入った時に、その時点の変数値で解決して固定される。直前のイベントの選択肢 `ops` や数値入力(input)でセットした値が、次のイベントの終了条件に反映される(例: 再生時間を数値入力させてdurationに埋め込む)。ステート再入では入り直すたびに解決し直す。
- **クランプ**: 解決値が範囲外の場合、`count` は1未満なら1に、`duration` の合計は0未満なら0に丸められる(0は「即時成立」= 再生中の音声を終えて終了)。transitionの `channel_count` も1未満は1に、`state_time` の負値は0に丸められ、min>max は入れ替えられる。
- 未宣言の変数・文字列変数の参照は読み込みエラー。静的な数値を書いた場合の検証(count≥1、duration>0)は従来どおり。
- 編集画面では各数値欄の隣の「x」トグルで数値入力⇔変数選択を切り替えられる(変数宣言があるときのみ表示)。変数指定のdurationは「分/秒」の単位を選択する。分+秒の併記など編集画面で表せない組み合わせは「変数指定」の読み取り専用表示になり、保存時にそのまま保持される。
- 選択肢の `timeout` への変数埋め込みは未対応(必要になった時点で追加予定)。

### 監視(watch)

変数条件を**常時監視**し、成立した瞬間に指定イベントへ遷移するトリガー。
トップレベルに配列で置き、シナリオ全体に効く(「HPが0になったら、どこにいても敗北イベントへ」)。

```json
"watch": [
  {"when": [{"var": "HP", "op": "<=", "value": 0}],
   "to": "defeat",
   "mode": "graceful",
   "once": true}
]
```

| 要素 | 型 | 必須 | 取りうる値・既定 |
|---|---|---|---|
| `when` | 配列 | 必須 | AND条件(判定式はcondと同じ形式)。全て成立で発火 |
| `to` | 文字列 | 必須 | 遷移先イベントID |
| `mode` | 文字列 | 任意 | `"graceful"`(既定)=**再生中の音声を最後まで終えてから**イベントを終了し遷移 / `"interrupt"`=即時打ち切りで遷移 |
| `once` | 真偽値 | 任意 | `true`(既定)=1回の再生につき1度だけ発火 / `false`=条件が成立するたび発火 |

**動作仕様**:
- 判定は変数操作(`on_start`/`on_end`/`on_play`/`on_complete`/選択肢`ops`/`on_timeout`)の**適用直後**に行われ、遅延なく「なった瞬間」に検知される。イベント入場時点で既に条件を満たしている場合も(操作の有無に関わらず)即発火する。
- **`to` のイベント再生中は、その監視を評価しない**(自明なループの防止)。`once: false` で「何度でも発火」させる場合は、遷移先で変数を回復させるか、cond分岐等で脱出経路を作ること。
- watch による遷移は**◀◀の履歴に積まれる**(敗北イベントから直前のイベントへ戻って再挑戦、が可能)。
- 選択肢との優先関係: 監視の発火は選択肢のデフォルト遷移・待機フェーズより優先される(タイムアウトの `on_timeout` でHPが尽きた場合、デフォルト先ではなく監視の遷移先へ進む)。
- 複数の監視が同時に成立した場合、1回の評価では**宣言順で先頭の1件のみ**発火する。未発火の監視は独立に残り、次の評価点(遷移先イベントへの入場時など)で発火し得る。
- graceful がイベント入場と同時に発火した場合、そのイベントの音声は再生されずに遷移する。
- イベント終了「無限」(`end: {"type":"none"}`・ステート形式の無限も同様)のイベントでも graceful は働く(全チャンネルが止まった時点で遷移する。以前はワインドダウン後に永久待機になる不具合があった)。

## チャンネル

キーは `"L"`(左) `"C"`(中央) `"R"`(右)。並行再生され、それぞれ独立の設定を持つ。

| 要素 | 型 | 必須 | 取りうる値・既定 |
|---|---|---|---|
| `mode` | 文字列 | 任意 | `"sequential"`(既定) / `"random"` / `"random_bag"`(下記) |
| `items` | 配列 | 必須 | アイテム(下記)。1つ以上 |
| `end` | オブジェクト | 任意 | 下表。省略時: sequential=**once**、random=エラー(※ステート内は無限) |

> **注意**: 通常イベントの sequential チャンネルで `end` を省略すると
> **「1周で終了」**になります。「移行/イベント終了まで無限に再生」したいときは
> **`{"type": "none"}` を明示**してください(編集画面で「無限(移行/終了まで)」を
> 選べば自動で書かれます。古いバージョンで保存したシナリオは `end` が
> 抜けている場合があるので、開いて保存し直すと直ります)。
| `interval` | 数値/オブジェクト | 任意 | アイテム間の待機秒。`0.5`(固定) or `{"min":0.5,"max":1.0}`(0.1秒刻み均等抽選)。既定0 |
| `pan` | オブジェクト | 任意 | `{"left":0.0〜1.0,"right":0.0〜1.0}`。既定はチャンネル既定(L=左寄せ/C=中央/R=右寄せ) |

**チャンネル終了条件**:

| type | 追加フィールド | 意味 | sequential | random |
|---|---|---|---|---|
| `"once"` | ― | 全アイテム1周で終了(既定) | ○ | × |
| `"repeat"` | `count`(整数≥1) | N周で終了 | ○ | × |
| `"duration"` | `seconds`(>0)。または `min_seconds`+`max_seconds`(範囲抽選) | 合計N秒で終了(再生中の音声は最後まで)。範囲指定はステートに入るたびに [min, max] の一様乱数を抽選(再入で再抽選。各値は定数 or `{"var":名}`。編集画面は「N秒で終了」を選ぶとコンボ下に min秒〜max秒 の行が出る・min==max は単一 `seconds` で保存) | ○ | ○ |
| `"plays"` | `count`(整数≥1) | そのchでN回再生したら終了。random+count=1で「どれか1曲抽選して終了」 | ○ | ○ |
| `"none"` | ― | 無限。イベントend=channel指定時、またはステート内のみ | ○ | ○ |

**`"random_bag"`(ランダム(重複なし))**: ランダムバッグ方式。全アイテムを
一通り再生し終えるまで同じアイテムを引かず、一巡したら全アイテムから抽選し直す
(補充。再生ログに「補充」の行が残る)。補充直後の抽選に制約はなく、次の周回の
最初に直前と同じアイテムが出ることもある(当初あった「直前アイテム回避」は
アイテム数2〜3で抽選が機械的な交互になるため撤去)。
`end` の扱い・許される type は `"random"` と同じ(上表の random 列)だが、
**`"repeat"`+`count` だけは追加で使える**(袋をN回使い切ったら終了=
「N周で終了」。1周=袋を1回使い切ること。残りが全て重み0以下になったときの
早めの補充も1周と数える。開始時から全アイテムが0以下のときは周と数えず
自然終了)。`count` は変数参照も可。
重み(`weight`)も同様に効く: 抽選のたびに解決し、0以下は抽選に出さない。
その周回の残りに引けるアイテムが無くなったら早めに補充し、全アイテムが0以下
ならチャンネル自然終了(通常のランダム再生と同じログ)。

ステート内では `end` 省略=`none` と同じ(移行/イベント終了まで無限)。

### スクリプト専用チャンネル(音声なしでデバイスだけ動かす)

全アイテムが**スクリプトのみアイテム**(`audio` 省略+`tracks`/`funscript` 明示)の
チャンネル。音声を再生せず、担当デバイス種別のスクリプトを独立したリズムで
実行する。コンテンツ(音声)のチャンネルと分離してデバイス制御を組みたい
用途向け(編集画面では「＋スクリプト」で作る。**funscript/CSV をチャンネル枠
(アイテムの外)へD&Dしても同じように追加できる**。アイテムの上へ落とした
場合は従来どおりそのアイテムのトラックになる。作ったチャンネルは
「他からコピー」のコピー元にも選べる)。

```json
"channels": {
  "C": {"items": ["voice.wav"]},
  "L": {"items": [{"tracks": [{"type": "linear", "funscript": "loop.funscript"}]}],
        "end": {"type": "none"}}
},
"device": {"linear": "L"},
"end": {"type": "channel", "channel": "C"}
```

- **アイテムの再生時間 = スクリプトの長さ**(全トラックの最終アクション時刻の
  最大)。チャンネル終了条件(1周/N周/N秒/N回/無限)・
  `channel_count`/`channel_end` 移行はこの時間・回数で従来どおり機能する。
- **制約(Scenario.load でエラー)**:
  - 1チャンネル内で音声つきアイテムとスクリプトのみアイテムは**混在不可**。
  - スクリプト専用チャンネルは**どれかのデバイス種別の担当**(`device`)で
    あること(担当が無いと何も起きないため)。
  - **スクリプト専用チャンネルだけのイベント/ステートに `end: plays`
    (N回の再生で次へ)は指定不可**(plays は音声・動画の本数しか数えないため
    永久に成立しない)。ステート形式では、どのステートにもコンテンツが無い
    場合にエラーになる。
  - スクリプトのみアイテムはトラック1つ以上必須・**長さ0のスクリプトは不可**
    (高速空回り防止。読み込み時に実ファイルを検査する)。
- **音声アイテムとの挙動差(仕様)**:
  - イベント終了条件・ステート移行の成立時(ワインドダウン)、音声は最後まで
    流れるがスクリプトは**即時中断**する(長尺スクリプトがイベント終了を
    遅らせない)。`on_complete` は最後まで流れた場合のみ発火。
  - イベント終了条件 `plays`(N回再生)にはスクリプトの再生を**数えない**
    (チャンネル自身の `plays`/`repeat` と `channel_count` 移行では数える)。
  - シーク(シークバー・±10秒)には追従しない(開始タイミングはイベント/
    ステート入場時に揃う)。音声にぴったり同期させたい場合は、従来どおり
    音声アイテム自体に `tracks` を付ける。**ただし自分自身が
    `seek_channel`(シークバー追従チャンネル)のときは追従する**。
  - `pan` はスクリプトのみアイテムでは無意味(指定しても音が無いため無効)。
- 再生タブの音声欄には代表トラックのスクリプトファイル名が表示される。

**スクリプト専用チャンネルだけのイベント/ステート**

音声・動画の素材が無くても、スクリプト専用チャンネルだけでイベント/ステートを
作れる(「コンテンツが1つ以上必要」という制約はない)。

```json
"pulse": {
  "channels": {"C": {"items": [{"tracks": [{"type": "linear",
                                            "funscript": "loop.funscript"}]}],
                     "end": {"type": "none"}}},
  "device": {"linear": "C"},
  "end": {"type": "duration", "seconds": 60},
  "next": "next_event"
}
```

- **シークバーはスクリプトに追従する**。`seek_channel` にスクリプト専用
  チャンネルを指定でき、無指定なら「音声のあるch → 無ければ担当ch」の順で
  自動決定する。経過時間・全長・±10秒送りはスクリプトの長さが基準になる。
- 使える終了条件は **省略(全チャンネル終了)/`duration`/`cond`/`channel`**。
  `plays` だけは上記のとおり指定不可。
- 編集画面では、終了条件が「合計時間」「変数条件」のとき**チャンネルの終了条件が
  「無限」のみ**に絞られる(有限だとスクリプトが終わった時点でイベントも
  終わってしまい、指定した秒数/条件まで持たないため)。

## アイテム(items配列の要素)

文字列 `"intro.wav"` と書くとオブジェクト `{"audio": "intro.wav"}` の省略形。

| 要素 | 型 | 必須 | 取りうる値・既定 |
|---|---|---|---|
| `audio` | 文字列 | ※ | wavファイルの相対/絶対パス(存在チェックあり)。**省略=スクリプトのみアイテム**(下記「スクリプト専用チャンネル」参照。`tracks` か `funscript` の明示指定が必須) |
| `tracks` | 配列 | 任意 | `[{"type":..., "funscript":...}, ...]` 複数デバイス対応(下記) |
| `funscript` | 文字列/null | 任意 | 旧形式: linear単一指定。null=デバイス連動なし |
| `weight` | 数値 or `{"var":..}` | 任意 | randomモードの抽選重み。既定1.0。**定数 or 数値変数参照**。**抽選のたび**(アイテムを引く直前)に解決し、**0以下の候補は抽選に出ない**(0/負の指定はエラーにしない)。**全アイテムが0以下ならチャンネル自然終了**(無限chでも終わる。再生ログに理由を記録) |
| `pan` | オブジェクト | 任意 | `{"left","right"}`。チャンネルpanより優先 |
| `on_play` | 配列 | 任意 | このアイテムの**再生開始時**に実行する変数操作(下記「変数」参照) |
| `on_complete` | 配列 | 任意 | このアイテムが**最後まで自然に再生された時**に実行する変数操作。▶▶スキップ・選択肢確定・停止による中断では発火しない(=「一定時間以上聞き続けたら成功」用途) |
| `range` | オブジェクト | 任意 | **区間指定**。`{"start": 秒, "end": 秒}`。素材の一部だけを使う(下記) |

**funscriptの決まり方(優先順)**:
1. `tracks` があればそれに従う
2. `funscript` キーがあれば linear 単一(nullなら連動なし)
3. どちらも無ければ**命名ルールで自動紐づけ**(大文字小文字無視):
   - 「wavファイル名(拡張子なし)を含む」`.funscript` が候補
   - ファイル名に `twist` を含む→twist / `ufo` を含む→rotate_ufo / `a10` を含む→rotate_a10cyclonesa /
     `vib` を含む→vibration / どのタグも含まない→linear
   - 例: `intro.wav` に `intro.funscript`(linear)+`intro_ufo.funscript`(rotate_ufo)+
     `intro.a10.funscript`(rotate_a10)+`intro_vib.funscript`(vibration)が同時に紐づく
   - 同一種別に候補が複数ある場合は決定的に1つ
     (linearは完全一致=同名を最優先、それ以外/同率はファイル名が短い順→辞書順)。
     該当なしなら連動なし

**tracksの要素**:

| 要素 | 型 | 必須 | 取りうる値 |
|---|---|---|---|
| `type` | 文字列 | 任意 | `"linear"`(既定) / `"twist"` / `"rotate_ufo"` / `"rotate_a10cyclonesa"` / `"vibration"` |
| `funscript` | 文字列 | 必須 | funscript **または CSV** ファイルのパス(rotate系はCSV可) |
| `range` | オブジェクト | 任意 | このトラックだけの**区間指定**。省略=アイテムの `range` に連動 |

※ 旧種別名 `"rotate"` は `"rotate_ufo"` の別名として読み込めます(既存シナリオの互換維持)。編集画面で保存すると新名 `"rotate_ufo"` で書き出されます。

funscriptのposの意味はデバイス種別で異なる:
linear=位置0-100 / twist=位置0-100(2軸linearデバイスの2軸目=有限回転。funscript仕様はlinearと同一) / rotate_ufo・rotate_a10cyclonesa=50停止・100正回転max・0負回転max / vibration=0停止・100最大振動。

**初動**: rotate系・vibrationのfunscriptで `at=0` のアクションが
無い場合、**最初のアクションの時刻に達するまでは停止**として駆動する
(rotate系=pos 50相当 / vibration=pos 0。既定 `pos,at=50,0` /
`pos,at=0,0` があるものとして扱う)。CSVも同じく最初の行まで停止。
区間指定の切り出しで先頭にギャップが生じる場合も同様に停止する
(「指示なし=停止」で一貫。linear/twistは位置デバイスのため対象外)。

**twist**: 2軸対応デバイス(linear+twist)の2軸目を駆動します。CSVは使えません(funscriptのみ)。1軸デバイスしか接続されていない場合、twistトラックは安全に何も送りません。再生画面・編集画面のTWIST関連UIは接続タブの「TWIST(2軸デバイスの2軸目)を使う」がONのときだけ表示されますが、OFFでもシナリオにtwistトラックがあれば再生は実行され、編集画面のトラック行にも表示されます(データが失われることはありません)。
`rotate_ufo`(ufosa/ufotw系)と `rotate_a10cyclonesa`(A10サイクロンSA系)は別レーンとして**分離**駆動される。既定ではデバイス名で振り分けられ(正規化して「a10cyclone」を含む機種→a10レーン、他→ufoレーン)、接続タブの「ROTATEデバイスの割り当て」で機種ごとに ufo/a10 のどちらで動かすか付け替えできる。

### 区間指定(range)

1本の長い素材の一部だけを切り出して使う。**音声・動画・スクリプトのどれでも**
指定できる(動画・音声・funscript/CSVに共通)。

```json
{"audio": "long.wav", "range": {"start": 10, "end": 20.5}}
{"video": "long.mp4", "range": {"start": 60, "end": 90}}
{"tracks": [{"type": "linear", "funscript": "long.funscript"}],
 "range": {"start": 30, "end": 45}}
```

| キー | 型 | 既定 | 意味 |
|---|---|---|---|
| `start` | 数値(秒) | 0 | 区間の開始。0以上。**小数可**(`12.5` など1秒未満も指定できる) |
| `end` | 数値(秒) | 素材の末尾 | 区間の終了。`start` より後 |

`true`/`false` は数値として受理しない(`True` が 1 と解釈される事故の防止)。

- **区間の先頭が0秒**として扱われる。経過時間・シークバー・
  10秒送り/戻し・funscript同期は、すべて区間の中だけを動く。
- **トラック(funscript/CSV)はアイテムの区間に連動する**。
  60秒の音声と60秒のfunscriptに `range: {"start":10,"end":20}` を指定すると、
  **両方の10〜20秒が一緒に切り出される**のでズレない。
  切り出したスクリプトは先頭が0msへずれる(`Funscript.sliced`)。
- **トラック側の `range` はアイテムの区間より優先される**。区間ごとに0秒起点で
  作った別のfunscriptを当てたいときはこちらを使う(省略=連動)。
- 動画トラックも同じ規則(初期のバージョンは「区間の先頭を0秒とした
  別funscript」前提だったので、**そのようなシナリオはトラック側の `range` を
  明示するか、funscriptを素材全体ぶんに作り直す**必要がある)。
- **スクリプト専用チャンネルの再生時間**: `end` を明示したアイテムは
  **区間の長さ(end−start)**が再生時間になる。スクリプトの最終アクションが
  区間の途中で終わっていても(例: 区間10〜20秒・CSVは16秒で停止行が最後)、
  **末尾の停止・保持区間まで実行してから**次の抽選へ進む。funscript/CSVの
  両方が対象。`end` を省略したとき(=素材の末尾)は従来どおり最終アクション
  時刻まで。音声・動画アイテムの再生時間は従来どおり音声・動画が決める。
- **検証(読み込み時)**: `start` が素材の長さ以上ならエラー。音声は wav の長さ、
  トラックはスクリプトの最終アクション時刻で判定する。スクリプト専用チャンネルの
  アイテムは、切り出した区間に動作が1つも無ければエラー(
  動作0のまま走ると初期指令が無く、直前のアイテムの動きが続いてしまうため)。
  **動画だけは長さが分からないので事前検証せず、再生時にログへ警告を出す**。
  なお「アイテムの区間に連動した結果トラックが空になる」のはエラーにしない
  (音声の後半にしかスクリプトが無い、といった正当なケースがあるため)。
- **編集画面**: チャンネルの「詳細設定」を開くと、アイテムとトラック行に
  「区間:」欄が出る(既定は非表示)。

### ROTATE用CSV(rotate_ufo / rotate_a10cyclonesa のみ)

rotate系トラックの `funscript` フィールドには、funscriptの代わりに **CSV** を指定できる(ROTATEはCSV制御が主流だった経緯に対応)。自動紐づけでも `.csv` を探索し、同名で `.funscript` と `.csv` の両方があれば **CSVを優先**する(CSVはROTATE系タグ=ufo/a10のみ対象。linear/vibrationはfunscriptのみ)。

CSVは時刻が**100ミリ秒単位**、ヘッダ行なし。列数で2形式を判別する:

- **タイプA(3列)**: `時刻, 方向(0=逆/1=正), 速度(0-100)`
- **タイプB(5列, ufotw専用)**: `時刻, 左方向, 左速度, 右方向, 右速度`(左右2ロータを独立制御)

各行の値は次の時刻まで保持(ステップ保持)。デバイスへの割り当て規則(N=CSVのチャンネル数, M=デバイスのロータ数):タイプA(1ch)は1ロータ機にそのまま/2ロータ機(ufotw)は両ロータへ複製。タイプB(2ch)は2ロータ機に左右個別(再生タブの「左右反転」で入替可)/1ロータ機(ufosa・a10)は速度の大きい側を採用(両方0で停止)。補正(出力レンジ・反転・動作タイミング)はレーン単位で全ロータへ同一適用。

## device(デバイス担当)

イベント/ステートごとに「どのデバイス種別がどのチャンネルの音声に同期するか」を指定。

| 書き方 | 意味 |
|---|---|
| 省略 | 全種別 → C(Cが無ければ最初のチャンネル) |
| `"device": "L"` | 全種別 → L(後方互換の文字列形式) |
| `"device": {"linear":"L","rotate_ufo":"R"}` | 種別ごとに指定。キーは `linear`/`rotate_ufo`(旧`rotate`も可)/`rotate_a10cyclonesa`/`vibration`、値は存在するchID。書かなかった種別は担当なし(=そのデバイスは動かない) |
| `"device": {"linear":"C"}`(C=動画ch) | **動画のトラックがその種別を駆動する**ことの明示。挙動は省略時と同じ(動画のトラックは device を見ずに必ず実行される) |

シークバー(経過表示・シーク操作)の追従チャンネルは `seek_channel` 指定 →
無指定なら C→L→R の優先順で音声のあるチャンネルに自動決定される
(指定chに音声が無い場合も同じ自動決定へフォールバック)。
device はデバイス駆動の担当のみを決め、シークバーには影響しない。

**動画チャンネルとの関係**: 動画アイテムのトラックは
動画クロックに同期して実行される。**その種別の担当が別のチャンネルにある
ときだけ実行されない**(そちらが駆動源になる)。担当が省略されている
種別は従来どおり動画が駆動するので、**旧シナリオは無改修で動く**。
動画chを指す担当(`{"linear":"C"}`)は省略と同じ意味で、
**「駆動源がどこか」を読んで分かるようにするための書き方**。

## 後方互換(旧形式)

すべて読み込み時に内部で新形式へ正規化される。

1. **v1 nodes形式**(トップレベルが `nodes`):
   ```json
   {"title":"...", "start":"intro",
    "nodes": {"intro": {"audio":"intro.wav", "funscript":"intro.funscript", "next":"main"},
              "main":  {"audio":"main.wav",  "funscript":null, "next":null}}}
   ```
   → 各ノードがCチャンネル1アイテムのイベントに変換。

2. **items直書きイベント**(イベント直下に `items`/`mode`/`end`/`interval`):
   → Cチャンネル1つのイベントに変換。

3. **文字列アイテム** / **funscript単一指定** / **device文字列** は上記の各表を参照。

## 最小構成の例

```json
{
  "title": "最小",
  "detail": "お姉さんがささやき声で日頃の疲れをいやしてあげます。",
  "start": "ev1",
  "events": {
    "ev1": {
      "channels": {
        "C": {"items": ["intro.wav"], "end": {"type": "once"}}
      },
      "next": null
    }
  }
}
```

## フル機能の例(ステート・複数ch・複数デバイス)

```json
{
  "title": "フル",
  "start": "intro",
  "events": {
    "intro": {
      "channels": {
        "C": {"mode": "random",
              "items": ["intro_a.wav", "intro_b.wav"],
              "end": {"type": "plays", "count": 1}}
      },
      "next": "main"
    },
    "main": {
      "start": "S1",
      "states": {
        "S1": {
          "channels": {
            "L": {"mode": "random",
                  "items": [{"audio": "v1.wav", "weight": 2.0},
                            {"audio": "v2.wav"}],
                  "interval": {"min": 0.5, "max": 1.0}},
            "R": {"mode": "sequential",
                  "items": [{"audio": "bgm.wav",
                             "tracks": [{"type": "rotate_ufo",
                                         "funscript": "bgm_rot.funscript"}]}]}
          },
          "device": {"linear": "L", "rotate_ufo": "R"},
          "transition": {
            "when": {"type": "state_time",
                     "min_seconds": 60, "max_seconds": 90},
            "to": {"random": ["S1", "S2"]}
          }
        },
        "S2": {
          "channels": {"C": {"items": ["calm.wav"]}},
          "transition": {"when": {"type": "channel_count",
                                  "channel": "C", "count": 2},
                         "to": "S1"}
        }
      },
      "end": {"type": "duration", "seconds": 600},
      "next": null
    }
  }
}
```
